- var meta_title = 'draw circle';
- var meta_description = 'draw by bezier curve.';

include ./_article.pug

+article
  p.c-article__summary
    |#{meta_description}
  .c-article__svg
    svg(width="500", height="500")
      path(
        id = 'transformer-path'
        stroke = "#333",
        stroke-width = "20px",
        fill = "none",
        d = ''
      )
